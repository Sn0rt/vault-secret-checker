apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "vault-secret-checker.serviceAccountName" . }}
  labels:
    {{- include "vault-secret-checker.labels" . | nindent 4 }}
automountServiceAccountToken: true